class DASHBOARD {
	private root = '/i'

	HOME = this.root
	SHORTS = `${this.root}/shorts`
	FAVORITES = `${this.root}/favorites`
	SETTINGS = `${this.root}/settings`
}

export const DASHBOARD_PAGES = new DASHBOARD()
